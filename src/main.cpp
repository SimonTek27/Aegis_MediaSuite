// Minimal main.cpp stub for building during refactor
#include <QApplication>
#include <QDebug>

int main(int argc, char *argv[]) {
    QApplication app(argc, argv);
    qDebug() << "Aegis build stub: full startup disabled during refactor.";
    return 0;
}
